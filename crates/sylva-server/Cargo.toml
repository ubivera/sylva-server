[package]
name = "sylva-server"
version = "0.0.1"
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Sylva Server binary: composes the gRPC + discovery + health surface"

[[bin]]
name = "sylva-server"
path = "src/main.rs"

[dependencies]
anyhow = { workspace = true }

server = { path = "../server" }

[lints]
workspace = true
